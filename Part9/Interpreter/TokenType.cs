namespace Interpreter
{
    public enum TokenType
    {
        INTEGER,
        PLUS,
        MINUS,
        MUL,
        DIV,
        LPAREN,
        RPAREN,
        ID,
        ASSIGN,
        BEGIN,
        END,
        SEMI,
        DOT,
        EOF,
    }
}